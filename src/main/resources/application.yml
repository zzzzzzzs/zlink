server:
  port: 5465

spring:
  application:
    name: ZLink
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/small_oms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto