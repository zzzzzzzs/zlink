<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>zlink</artifactId>
        <groupId>work.sajor</groupId>
        <version>0.0.1</version>
    </parent>

    <groupId>work.sajor</groupId>
    <artifactId>zlink-admin</artifactId>
    <version>${zlink.verzion}</version>
    <name>zlink-admin</name>
    <description>zlink-admin</description>
    <properties>
        <java.version>11</java.version>
    </properties>

    <dependencies>

        <dependency>
            <groupId>work.sajor</groupId>
            <artifactId>crap-server</artifactId>
            <version>${crap.version}</version>
        </dependency>

        <dependency>
            <groupId>work.sajor</groupId>
            <artifactId>zlink-modules</artifactId>
            <version>${zlink.verzion}</version>
        </dependency>

        <dependency>
            <groupId>work.sajor</groupId>
            <artifactId>zlink-manager</artifactId>
            <version>${zlink.verzion}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
